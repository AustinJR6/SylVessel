Preferred runtime tool behavior:

- Use `work_sessions` for autonomous background tasks.
- Use `web_search` when recency matters.
- Use `github` and `code_execution` for implementation work instead of describing hypothetical steps.
- Use proactive notes for deferred summaries and heartbeat review.
